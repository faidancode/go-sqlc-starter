version: "2"
sql:
  - schema: "db/schemas"
    queries: "db/migrations"
    engine: "postgresql"
    gen:
      go:
        package: "dbgen"
        out: "internal/dbgen"
        emit_json_tags: true
        emit_prepared_queries: true
