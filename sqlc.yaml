version: "2"
sql:
  - schema: "db/schema.sql"
    queries: "db/query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "dbgen"
        out: "internal/dbgen"
        emit_json_tags: true
        emit_prepared_queries: true
